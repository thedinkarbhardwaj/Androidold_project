package com.example.dummy.AnimationDemo

import android.os.Bundle
import android.widget.ImageView
import androidx.appcompat.app.AppCompatActivity
import com.bumptech.glide.Glide
import com.example.dummy.R


class AnimationActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_animation)

//        val videoView = findViewById<VideoView>(R.id.videoview)
//
//        // Specify the path or URI of the video file
//
//        // Specify the path or URI of the video file
//        val videoPath = "android.resource://" + packageName + "/" + R.raw.video
//
//        val uri = Uri.parse(videoPath)
//
//        // Set the video URI to the VideoView
//
//        // Set the video URI to the VideoView
//        videoView.setVideoURI(uri)

        // Add media controller to enable play, pause, etc.

        // Add media controller to enable play, pause, etc.
//        val mediaController = MediaController(this)
//        mediaController.setAnchorView(videoView)
//        videoView.setMediaController(mediaController)

        // Start playing the video

        // Start playing the video
    //    videoView.start()


//        val imageView = findViewById<ImageView>(R.id.imageView)

        // Replace "your_gif_url_here" with the actual URL of your GIF image


        // Replace "your_gif_url_here" with the actual URL of your GIF image

        // Replace "your_gif_url_here" with the actual URL of your GIF image

//        val resourceId = resources.getIdentifier("splashanimation", "raw", packageName)
//
//        // Use Glide to load and display the GIF image
//
//        // Use Glide to load and display the GIF image
//        Glide.with(this)
//            .asGif()
//            .load(resourceId)
//            .into(imageView)

    }
}